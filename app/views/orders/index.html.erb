<div class="page">
  <h1>Orders</h1>

  <div class="form-group">
    <%= form_tag(orders_path, method: "get") do %>
      <%= label_tag(:q, "Search By Movie Title:") %>
      <%= text_field_tag :search, params[:search] %>
        <%= submit_tag "Search", :name => nil, class: "btn btn-primary btn-sm" %>
    <% end %>
    <div >
       <%= form_tag(orders_path, method: "get") do %>
          <%= submit_tag "Refresh", :name => nil, class: "btn btn-default btn-sm" %>
      <% end %>
    </div>
  </div>


  <div>
    <table class="table">
      <tr>
         <th>Order Number</th>
         <th>Order Date</th>
         <th>Order Total</th>
         <th>Movie Title</th>
      </tr>
      <% @orders.each do |order| %>
        <tr>
          <td><%= order.id %></td>
          <td><%= order.created_at %></td>
          <td><%= order.total %></td>
          <td><%= order.showtime.movie.title %></td>
        </tr>
      <% end %>
    </table>
  </div>
</div>